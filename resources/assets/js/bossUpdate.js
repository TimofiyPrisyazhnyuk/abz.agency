$.ajaxSetup({    headers: {        'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')    }});$(document).ready(function () {    bossUpdate();});$(document).on('change', '#position_id', function () {    bossUpdate();});function bossUpdate() {    let positionId = $('#position_id').val();    let boss = $('#boss_id');    boss.find('option').remove();    console.log($('#parentBoss').val());    $.ajax({        type: "POST",        url: '/staff_list/boss',        data: {'position_id': positionId},        success: function (data) {            if (data.length === 0) {                boss.append('<option value="null" selected="" disabled="">Boss Name</option>');            } else {                for (let i = 0; i < data.length; i++) {                    if (data[i].id == $('#parentBoss').val()) {                        boss.append('<option selected value="' + data[i].id + '" >' +                            (data[i].surname + ' ' + data[i].first_name + ' ' + data[i].patronymic).slice(0, 14) + '...' + '</option>');                    } else {                        boss.append('<option value="' + data[i].id + '" >' +                            (data[i].surname + ' ' + data[i].first_name + ' ' + data[i].patronymic).slice(0, 14) + '...' + '</option>');                    }                }            }            // console.log($('#parentBoss').val());        }    });}